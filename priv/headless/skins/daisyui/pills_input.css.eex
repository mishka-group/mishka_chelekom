/* pills_input — daisyUI skin.
 *
 * The box is daisyUI's `input`, but it has to grow: `input` is a fixed-height row and a tag field
 * wraps onto several. So the box is drawn from the same tokens with `height: auto`, and each pill
 * is a `badge` — which is what a pill is.
 */

.chelekom-pills-input {
  border-radius: var(--radius-field);
  border: var(--border) solid color-mix(in oklab, var(--color-base-content) 20%, #0000);
  background-color: var(--color-base-100);
  color: var(--color-base-content);
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: calc(0.25rem * 1.5);
  padding: calc(0.25rem * 1.5);
  min-height: calc(var(--size-field, 0.25rem) * 10);
}

.chelekom-pills-input:focus-within {
  outline: 2px solid var(--color-base-content);
  outline-offset: 2px;
}




/* The field grows to fill whatever the tags leave, so clicking the empty space still focuses it. */
.chelekom-pills-input__input {
  flex: 1;
  min-width: 6rem;
  border: none;
  background: transparent;
  outline: none;
  font-size: 0.875rem;
}
